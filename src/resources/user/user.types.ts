interface UserTypes {
    name: string,
    role:string,
    password:string
}

interface UserTypes {
    email:string
}

interface UserTypes {
    token:string
}

interface UserTypes {
    acc_status:any,
    active_status: boolean
}
export {UserTypes}